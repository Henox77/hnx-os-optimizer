<UserControl x:Class="HNXOSOptimizer.ToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HNXOSOptimizer"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="780"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <!-- Custom TabControl styling for Dark Theme -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" 
                                Background="#1E1E2A" 
                                BorderBrush="{StaticResource BorderBrush}" 
                                BorderThickness="1,1,1,0" 
                                CornerRadius="5,5,0,0" 
                                Padding="15,8" 
                                Margin="0,0,5,0"
                                Cursor="Hand">
                            <ContentPresenter x:Name="ContentSite"
                                              ContentSource="Header"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentPrimary}"/>
                                <Setter Property="Panel.ZIndex" Value="100"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsSelected" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Border" Property="Background" Value="#2A2A3A"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsTitle]}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" FontFamily="{StaticResource MainFont}"/>
            <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsSubtitle]}" FontSize="13" Foreground="{StaticResource TextSecondary}" FontFamily="{StaticResource MainFont}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Tabbed Tools Container -->
        <TabControl Grid.Row="1">
            
            <!-- Tab 1: Registry Fix & Context Menu -->
            <TabItem Header="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsTabReg]}">
                <Grid Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.1*"/>
                        <ColumnDefinition Width="0.9*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Registry Cleaner (Left) -->
                    <Border Background="{StaticResource BgCard}" CornerRadius="10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,8,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                                <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsRegIssues]}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                                <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsRegIssuesDesc]}" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>

                            <!-- Issue List -->
                            <ListBox x:Name="ListRegIssues" Grid.Row="1" Background="#12121A" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="5" Margin="0,0,0,10">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,3">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Type}" Foreground="{StaticResource AccentCyan}" FontWeight="Bold" FontSize="12"/>
                                                <TextBlock Text="{Binding Detail}" Foreground="#FFFFFF" FontSize="11" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding Path}" Foreground="{StaticResource TextMuted}" FontSize="9"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <!-- Action Buttons -->
                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button x:Name="BtnScanReg" Grid.Column="0" Content="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsBtnScan]}" Style="{StaticResource SecondaryButton}" Margin="0,0,5,0" Click="BtnScanReg_Click"/>
                                <Button x:Name="BtnFixReg" Grid.Column="1" Content="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsBtnFix]}" Style="{StaticResource ModernButton}" Margin="5,0,0,0" Click="BtnFixReg_Click" IsEnabled="False"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- Context Menu Editor (Right) -->
                    <Border Grid.Column="1" Background="{StaticResource BgCard}" CornerRadius="10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="15" Margin="8,0,0,0">
                        <StackPanel>
                            <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsContextTitle]}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,12"/>
                            
                            <!-- CMD Toggle -->
                            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsContextCmd]}" FontSize="13" FontWeight="Bold" Foreground="#FFFFFF"/>
                                        <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsContextCmdDesc]}" FontSize="10" Foreground="{StaticResource TextSecondary}"/>
                                    </StackPanel>
                                    <ToggleButton x:Name="ToggleCmdMenu" Grid.Column="1" Style="{StaticResource ToggleSwitch}" Click="ToggleCmdMenu_Click"/>
                                </Grid>
                            </Border>

                            <!-- Notepad Toggle -->
                            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsContextNotepad]}" FontSize="13" FontWeight="Bold" Foreground="#FFFFFF"/>
                                        <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsContextNotepadDesc]}" FontSize="10" Foreground="{StaticResource TextSecondary}"/>
                                    </StackPanel>
                                    <ToggleButton x:Name="ToggleNotepadMenu" Grid.Column="1" Style="{StaticResource ToggleSwitch}" Click="ToggleNotepadMenu_Click"/>
                                </Grid>
                            </Border>

                            <!-- PowerShell Toggle -->
                            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsContextPs]}" FontSize="13" FontWeight="Bold" Foreground="#FFFFFF"/>
                                        <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsContextPsDesc]}" FontSize="10" Foreground="{StaticResource TextSecondary}"/>
                                    </StackPanel>
                                    <ToggleButton x:Name="TogglePsMenu" Grid.Column="1" Style="{StaticResource ToggleSwitch}" Click="TogglePsMenu_Click"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Tab 2: PATH Environment variables -->
            <TabItem Header="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsTabPath]}">
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{StaticResource BgCard}" CornerRadius="10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                                <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsPathTitle]}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                                <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsPathDesc]}" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>

                            <ListBox x:Name="ListPaths" Grid.Row="1" Background="#12121A" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="5">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding}" Foreground="#FFFFFF" VerticalAlignment="Center" FontSize="12"/>
                                            <Button Grid.Column="1" Content="Kaldır" Style="{StaticResource SecondaryButton}" Padding="8,3" FontSize="10" Margin="10,0,0,0" Click="BtnRemovePath_Click" Tag="{Binding}"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>

                    <Border Grid.Row="1" Background="{StaticResource BgCard}" CornerRadius="10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TxtNewPath" Background="#12121A" Foreground="#FFFFFF" BorderBrush="{StaticResource BorderBrush}" VerticalContentAlignment="Center" Padding="8,5" BorderThickness="1" Margin="0,0,10,0"/>
                            <Button x:Name="BtnAddPath" Grid.Column="1" Content="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsPathBtnAdd]}" Style="{StaticResource ModernButton}" Click="BtnAddPath_Click"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Tab 3: Hardware Inspector -->
            <TabItem Header="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsTabHw]}">
                <Border Background="{StaticResource BgCard}" CornerRadius="10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="20" Margin="0,15,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsHwTitle]}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,15"/>
                        
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <!-- CPU Details -->
                                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,10">
                                    <Grid>
                                        <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[HomeCPU]}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource TextSecondary}"/>
                                        <TextBlock x:Name="TxtInfoCpu" Text="Intel Core i7 / AMD Ryzen" FontSize="13" Foreground="#FFFFFF" HorizontalAlignment="Right" TextAlignment="Right" Width="450" TextWrapping="Wrap"/>
                                    </Grid>
                                </Border>

                                <!-- GPU Details -->
                                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,10">
                                    <Grid>
                                        <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[HomeGPU]}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource TextSecondary}"/>
                                        <TextBlock x:Name="TxtInfoGpu" Text="NVIDIA GeForce / AMD Radeon" FontSize="13" Foreground="#FFFFFF" HorizontalAlignment="Right" TextAlignment="Right" Width="450" TextWrapping="Wrap"/>
                                    </Grid>
                                </Border>

                                <!-- Memory Details -->
                                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,10">
                                    <Grid>
                                        <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[HomeRAM]}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource TextSecondary}"/>
                                        <TextBlock x:Name="TxtInfoRam" Text="16 GB RAM" FontSize="13" Foreground="#FFFFFF" HorizontalAlignment="Right"/>
                                    </Grid>
                                </Border>

                                <!-- Disk Details -->
                                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,10">
                                    <Grid>
                                        <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[HomeDisk]}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource TextSecondary}"/>
                                        <TextBlock x:Name="TxtInfoDisk" Text="C:\ D:\" FontSize="13" Foreground="#FFFFFF" HorizontalAlignment="Right" TextAlignment="Right" Width="450" TextWrapping="Wrap"/>
                                    </Grid>
                                </Border>

                                <!-- OS Version Details -->
                                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,10">
                                    <Grid>
                                        <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[HomeOS]}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource TextSecondary}"/>
                                        <TextBlock x:Name="TxtInfoOs" Text="Windows 11 Pro 23H2" FontSize="13" Foreground="#FFFFFF" HorizontalAlignment="Right" TextAlignment="Right" Width="450" TextWrapping="Wrap"/>
                                    </Grid>
                                </Border>

                                <!-- Architecture Details -->
                                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,10">
                                    <Grid>
                                        <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[Arch]}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource TextSecondary}"/>
                                        <TextBlock x:Name="TxtInfoArch" Text="x64" FontSize="13" Foreground="#FFFFFF" HorizontalAlignment="Right"/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Tab 4: File Lock Finder & Unlocker -->
            <TabItem Header="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsTabLock]}">
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{StaticResource BgCard}" CornerRadius="10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,12">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,8">
                                <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsLockTitle]}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                                <TextBlock Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsLockDesc]}" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>
                            
                            <TextBox x:Name="TxtFilePath" Grid.Row="1" Grid.Column="0" Background="#12121A" Foreground="#FFFFFF" BorderBrush="{StaticResource BorderBrush}" VerticalContentAlignment="Center" Padding="8,5" BorderThickness="1" Margin="0,0,10,0"/>
                            <Button x:Name="BtnBrowseFile" Grid.Row="1" Grid.Column="1" Content="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsLockBtnBrowse]}" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Click="BtnBrowseFile_Click"/>
                            <Button x:Name="BtnScanLock" Grid.Row="1" Grid.Column="2" Content="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsLockBtnScan]}" Style="{StaticResource ModernButton}" Click="BtnScanLock_Click"/>
                        </Grid>
                    </Border>

                    <!-- Locking Processes List -->
                    <Border Grid.Row="1" Background="{StaticResource BgCard}" CornerRadius="10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsLockListTitle]}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                            <ListBox x:Name="ListLockingProcesses" Grid.Row="1" Background="#12121A" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="5">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,3">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding ProcessName}" Foreground="#FFFFFF" FontWeight="Bold" FontSize="13"/>
                                                <TextBlock Text="{Binding MainModule.FileName}" Foreground="{StaticResource TextMuted}" FontSize="10" TextTrimming="CharacterEllipsis" Width="400"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="PID: " Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                                                <TextBlock Text="{Binding Id}" Foreground="{StaticResource AccentCyan}" FontWeight="Bold" FontSize="11" Margin="0,0,10,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>

                    <Button x:Name="BtnUnlock" Grid.Row="2" Content="{Binding Source={x:Static local:LocalizationProvider.Instance}, Path=[ToolsLockBtnUnlock]}" Style="{StaticResource ModernButton}" Background="{StaticResource AccentSecondary}" Click="BtnUnlock_Click" IsEnabled="False"/>
                </Grid>
            </TabItem>
            
        </TabControl>
    </Grid>
</UserControl>
